
module.exports = function (req, res, errObject, newName, cb) {

  var newName = {}

  console.log('>>>>>>>>>>>>>>>>> modifyErrorObjectKeys <<<<<<<<<<<<<<<<<')

  /*
    Object.keys(req.body).forEach(function(k) {
      if(k === 'newUserDataItem'){
        newUserDataItem = true
        nk = req.body.type
        req.body[nk] = req.body[k];
        delete req.body[k];
      }
      if(k === 'confirmNewUserDataItem'){
        newUserDataItem = true
        nk = confirmK
        req.body[nk] = req.body[k];
        delete req.body[k];
      }
    })
  */


}
